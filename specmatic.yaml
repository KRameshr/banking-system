version: 3
systemUnderTest:
  service:
    $ref: "#/components/services/bankingService"
specmatic:
  settings:
    test:
      schemaResiliencyTests: all
components:
  services:
    bankingService:
      description: ValueMeters Banking API
      definitions:
        - definition:
            source:
              filesystem:
                directory: .
            specs:
              - spec:
                  path: openapi.json
                  type: openapi